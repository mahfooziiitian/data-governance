/*

HCatalog provides Apache Hive, Apache Pig and MapReduce with a consistent interface.
HCatalog's objective is to enable Pig and MapReduce to use the same data structures as Hive.
Then the data need not be converted.
HCatalog is a storage and management layer for Hadoop that enables users with different data processing tools like Pig, 
MapReduce, and Hive.
HCatalog table abstraction presents users with a relational view of the data in the HDFS.
HCatalog table abstraction presents users with a relational view of the data in the HDFS.
HCatalog supports the reading and writing of files in any format for which a is writable.
 */
package com.mahfooz.hcatalog;

/**
 *
 * @author 370037
 */
public class Hcatalog {
    
}
